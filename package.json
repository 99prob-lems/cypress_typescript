{
  "name": "cypress_typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:typescript": "rm -rf ./path-to-your-typescript-output-directory",
    "clean:node_modules": "rm -rf node_modules && npm cache clean --force",
    "reinstall": "npm run clean:node_modules && npm install",
    "test:feature": "cypress run --spec 'cypress/integration/path-to-your-feature-file.feature'",
    "test:headless": "cypress run --headless",
    "test:parallel": "cypress run --parallel --record --key YOUR_RECORD_KEY"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.2",
    "@badeball/cypress-cucumber-preprocessor": "latest",
    "@cypress/webpack-preprocessor": "latest",
    "ts-loader": "latest",
    "typescript": "latest" 
  }
}
